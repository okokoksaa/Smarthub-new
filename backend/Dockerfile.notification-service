# Multi-stage build for Notification Service
FROM node:20-alpine AS builder

WORKDIR /app

RUN npm install -g pnpm

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY services/notification-service/package.json ./services/notification-service/
COPY shared/database/package.json ./shared/database/

RUN pnpm install --frozen-lockfile

COPY shared/ ./shared/
COPY services/notification-service/ ./services/notification-service/

RUN pnpm --filter @shared/database build
RUN pnpm --filter notification-service build

# Production stage
FROM node:20-alpine

RUN apk add --no-cache dumb-init

RUN addgroup -g 1001 -S nodejs && \
    adduser -S nodejs -u 1001

WORKDIR /app

RUN npm install -g pnpm

COPY --chown=nodejs:nodejs package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY --chown=nodejs:nodejs services/notification-service/package.json ./services/notification-service/
COPY --chown=nodejs:nodejs shared/database/package.json ./shared/database/

RUN pnpm install --frozen-lockfile --prod

COPY --chown=nodejs:nodejs --from=builder /app/shared/database/dist ./shared/database/dist
COPY --chown=nodejs:nodejs --from=builder /app/services/notification-service/dist ./services/notification-service/dist
COPY --chown=nodejs:nodejs services/notification-service/templates ./services/notification-service/templates

USER nodejs

EXPOSE 3005

HEALTHCHECK --interval=30s --timeout=5s --start-period=30s --retries=3 \
  CMD node -e "require('http').get('http://localhost:3005/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"

ENTRYPOINT ["dumb-init", "--"]
CMD ["node", "services/notification-service/dist/main.js"]
